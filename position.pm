sub new() {}

sub current() {
	#return current position
}

sub next() {
	#return next position
}
sub previous() {
	#return previous position
}

sub upper_left() {
	#return upper left position if available
}

sub upper() {
	#return upper position if available
}

sub upper_right() {
	#return upper right position if available
}

sub left() {}
sub right() {}

sub lower_left() {}
sub lower() {}
sub lower_right() {}

sub piece() {}
